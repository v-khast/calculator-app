import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  result: {
    color: '#fff',
    fontSize: 48,
    padding: 4,
    textAlign: 'right',
  },
});

export default styles;

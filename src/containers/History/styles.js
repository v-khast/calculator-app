import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  historyWrapper: {
    height: 88,
    borderBottomWidth: 1,
    borderBottomColor: '#008a71',
  },
  historyItem: {
    padding: 10,
    fontSize: 18,
    color: '#fff',
    textAlign: 'right',
  },
});

export default styles;
